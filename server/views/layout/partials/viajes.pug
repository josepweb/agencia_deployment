.row.proximos-viajes
            each viaje in viajes
                .col-md-6.col-lg-4.mb-4
                    .card
                        img.img-fluid(src=`img/destinos_${viaje.imagen}.jpg`)
                        .card-body
                            h2=viaje.titulo
                            p  
                                i.fas.fa-calendar-alt   
                                | #{viaje.fecha_ida.toDateString()} - #{viaje.fecha_vuelta.toDateString()}
                            p  
                                i.fas.fa-dollar-sign 
                                | #{viaje.precio}
                            p= viaje.descripcion.substr(0,100)
                            a(href=`viajes/${viaje.id}`,class="btn btn-success btn-block") Más Información